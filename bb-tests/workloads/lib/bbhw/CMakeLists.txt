# bbhw: ISA macros (headers) + mem.c (bb_dma_touch).
# Include path is WORKLOAD_LIB_DIR (workloads/lib), so #include <bbhw/...> works.
# mem.c is compiled into each ctest via bb-tests/workloads/CMakeLists.txt.
