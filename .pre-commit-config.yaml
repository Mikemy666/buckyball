# Prevent pre-commit from checking unstaged files
exclude: ^$
repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v19.1.3
    hooks:
      - id: clang-format
        name: clang-format C++ code
        language: system
        files: \.(cpp|hpp|cc|cxx|h|c|hxx)$
        args: [--style=llvm]

  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black
        language: system

  # a comprehensive tool for checking the style and quality of Python code.
  # It combines three popular Python tools:
  # PyFlakes: Checks for logical errors in the code.
  # pycodestyle (formerly known as pep8): Checks for adherence to the PEP 8 style guide.
  # McCabe Complexity Checker: Measures the complexity of your code.
  - repo: https://github.com/PyCQA/flake8
    rev: 6.1.0
    hooks:
      - id: flake8
        language: system
        exclude: (^|/)bbdev/api/steps/config/scripts/chip_pb2\.py$
        args:
          - --max-line-length=120  # Adjust as per your style guide
          - --ignore=F821,F403,F405,F401,W503,E203,E402,E401,W605,E712,E711,F841
          # W503: Line break before binary operator
          # E203: Whitespace before colon
          # E402: Module level import not at top of file
          # E401: Multiple imports on one line
          # W605: Invalid escape sequence
          # E712: Comparison to True/False
          # E711: Comparison to None
          # F841: Unused variable

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: end-of-file-fixer
        language: system
      - id: trailing-whitespace
        language: system
      - id: check-merge-conflict
        language: system
      - id: check-yaml
        language: system
      - id: check-added-large-files
        language: system

  # Scala formatting with scalafmt
  - repo: local
    hooks:
      - id: verible-verilog-format
        name: Format Verilog/SystemVerilog code with verible
        entry: verible-verilog-format
        language: system
        files: \.(v|vh|sv|svh)$
        exclude: verify/src/common/.*_defs\.svh$
        args:
          - --inplace
          - --failsafe_success=false
          - --indentation_spaces=2

      - id: scalafmt
        name: Format Scala code with scalafmt
        entry: scalafmt
        language: system
        files: \.scala$
        exclude: (^|/)(thirdparty|node_modules)/
